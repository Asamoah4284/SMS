# Server
PORT=5000
NODE_ENV=development

# Database (PostgreSQL)
# Local Docker/Postgres (default dev)
DATABASE_URL="postgresql://sms_user:sms_password@localhost:5432/sms_db"

# Optional: raise Prisma pool if you see P2024 (connection pool timeout) under load / remote DB
# DATABASE_CONNECTION_LIMIT=15
# DATABASE_POOL_TIMEOUT=20

# Remote Neon example (optional)
# DATABASE_URL="postgresql://<user>:<password>@<your-neon-pooler-host>:5432/<db>?sslmode=require&pgbouncer=true&connect_timeout=15"

# JWT Secrets
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters
REFRESH_TOKEN_SECRET=your_refresh_token_secret_key_here_minimum_32_characters

# Frontend URL (SMS invite links, and CORS when CORS_ORIGINS is unset)
FRONTEND_URL=http://localhost:3000

# Optional: exact CORS allowlist (comma-separated). If set, replaces auto list (localhost + FRONTEND_URL).
# CORS_ORIGINS=http://localhost:3000,https://your-app.netlify.app

# ─── School Identity (edit per deployment) ───────────────────────────────────
SCHOOL_NAME="My School"
SCHOOL_ABBREVIATION="MYSCH"
SCHOOL_MOTTO="Knowledge is Power"
SCHOOL_ADDRESS="123 School St, Accra"
SCHOOL_REGION="Greater Accra"
SCHOOL_DISTRICT="Accra Metro"
SCHOOL_PHONE="0200000000"
SCHOOL_EMAIL="info@myschool.edu.gh"

# ─── SMS Gateway ──────────────────────────────────────────────────────────────
# Provider: "hubtel" or "arkesel"
SMS_PROVIDER=hubtel

# Hubtel
HUBTEL_CLIENT_ID=your_hubtel_client_id
HUBTEL_CLIENT_SECRET=your_hubtel_client_secret
HUBTEL_SENDER_ID=EduTrack

# Arkesel (alternative)
# ARKESEL_API_KEY=your_arkesel_api_key
# ARKESEL_SENDER_ID=EduTrack

# ─── Paystack (legacy — no longer used for fees; kept for old webhooks if needed) ─
# PAYSTACK_SECRET_KEY=

# ─── Moolre (parent app fee + book payments — MoMo embed checkout) ────────────
# Same vendor as StarExpress payments. SMS may use MOOLRE_API_KEY separately.
# Dashboard / Moolre: username + public key + wallet account number
MOOLRE_USERNAME=
MOOLRE_PUBLIC_KEY=
MOOLRE_ACCOUNT_NUMBER=
# Optional: validate wallet callbacks
# MOOLRE_WEBHOOK_SECRET=
# MOOLRE_WEBHOOK_ALLOW_MISSING_SECRET=true
# Public HTTPS base of this API (required for live callbacks / redirects)
# BACKEND_URL=https://api.yourschool.com
# Optional overrides:
# MOOLRE_PAYMENT_CALLBACK_URL=https://api.yourschool.com/api/v1/webhooks/moolre
# MOOLRE_REDIRECT_URL=https://api.yourschool.com/api/v1/webhooks/moolre/payment-success
# Platform commission on parent payments (parent pays gross = school amount + fee)
# FEE_COMMISSION_RATE=0.015
# BOOK_COMMISSION_RATE=0.05

# Phone number(s) to SMS when a school admin requests a payout (comma-separated Ghana numbers)
# PAYOUT_ALERT_PHONES=0542343069,0244100001

# ─── File Storage ─────────────────────────────────────────────────────────────
UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=5
