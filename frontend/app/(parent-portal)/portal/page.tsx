export const metadata = { title: 'My Children — EduTrack Parent Portal' };

export default function ParentPortalPage() {
  return (
    <div>
      <h2 className="text-xl font-bold mb-4">My Children</h2>
      {/* TODO: list children with quick stats — attendance, results, fee status */}
    </div>
  );
}
